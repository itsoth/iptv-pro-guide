---
description: Build the Next.js project with static export
agent: build-agent
---

Run `npm run build` in the project root. If it fails, fix any errors and rebuild until it succeeds. If it succeeds, verify the `out/` directory has all expected pages.
